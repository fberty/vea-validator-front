# VEA Validator

Rust validator for the VEA cross-chain messaging protocol (Arbitrum ↔ Ethereum).

## Quick Start

```bash
# Install dependencies (Rust & Foundry)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
curl -L https://foundry.paradigm.xyz | bash && foundryup

# Fetch contracts & start local devnet with deployed contracts
./scripts/fetch-vea-contracts.sh
./scripts/full-devnet.sh

# Run tests
cargo test
```

> todo: run validator

> todo: docker or equivalent

## Project Structure

```
├── src/                # Rust validator
├── tests/              # Rust validator tests
├── contracts/          # VEA contracts (fetched)
├── scripts/            # Setup scripts
└── info/               # Internal docs
```

## Scripts

- `fetch-vea-contracts.sh` - Downloads VEA contracts from GitHub
- `full-devnet.sh` - Starts local devnet + deploys all contracts (Ctrl+C to stop)

## Configuration

The `.env` file is auto-generated by `full-devnet.sh` with:
- RPC URLs for local Arbitrum/Mainnet
- Deployed contract addresses

> todo: figure out key management on launch

## Development

```bash
cargo run   # Run validator
cargo test  # Run tests
```

## License

MIT